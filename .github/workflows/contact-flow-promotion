name: Flow Promotion -stage to train
on:
  workflow_dispatch:
jobs:
  copy-to-branches:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: promotion flow
        run: |
          echo "Promotion flow from stage to train"
